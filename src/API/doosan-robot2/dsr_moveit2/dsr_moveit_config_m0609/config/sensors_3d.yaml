# The name of this file shouldn't be changed, or else the Setup Assistant won't detect it
#
# MoveIt occupancy map monitor (Octomap) — eye-in-hand RealSense D435.
# MoveItConfigsBuilder.to_moveit_configs() 가 config/sensors_3d.yaml 을 자동으로 읽어
# move_group 파라미터에 합치므로 start.launch.py 는 건드리지 않는다.
#
# 단위는 MoveIt 네이티브인 m 이다(프로젝트 mm 규약은 Python pose API 에만 적용).
sensors:
  - realsense_d435

realsense_d435:
  sensor_plugin: occupancy_map_monitor/PointCloudOctomapUpdater

  # kit_robot motion.py 가 중계하는 게이트 토픽(motion.yaml: moveit.octomap.cloud_out).
  # 로봇 없이 항상 매핑하고 싶으면 /camera/depth/color/points 로 바꾼다.
  point_cloud_topic: /kit/octomap_cloud

  # D435 는 1.5 m 를 넘으면 노이즈가 급증한다. 작업영역(테이블 + 트레이)만 담는다.
  max_range: 1.2

  point_subsample: 2

  # 로봇 자신·그리퍼를 걸러낼 때 링크 형상을 얼마나 부풀릴지. m 단위 offset.
  # "안전거리"에 해당하는 옥토맵 옵션은 이거 하나뿐이라 기술적 바닥까지
  # 낮췄다 — offset 0, scale 1.0 은 SRDF 링크 형상 그대로(추가 여유 없음).
  # 트레이드오프: 이전엔 부풀린 만큼 그리퍼 주변 진짜 장애물(집을 물체,
  # 트레이 벽)도 "로봇 자신"으로 같이 걸러져 사라졌었다 — 지금은 그 점들이
  # 살아남아 보이는 대신, hand-eye 캘리브레이션이 조금만 틀려도 그리퍼
  # 표면 자체가 voxel 로 잡힐 여지가 생긴다. RViz 에서 그리퍼 자세별로
  # filtered_cloud_topic 을 보고 self-filter 가 여전히 제 몸을 잘 걷어내는지
  # 확인할 것 — 계속 그리퍼가 자기 자신을 가리면 scale 을 다시 조금 올린다.
  padding_offset: 0.0
  padding_scale: 1.0

  max_update_rate: 5.0

  # 셀프필터 통과분. RViz PointCloud2 로 붙여서 캘리브레이션 검증에 쓴다.
  filtered_cloud_topic: /kit/octomap_filtered_cloud

# 고정 프레임이어야 한다. keepout box 와 같은 frame 을 쓴다(motion.yaml: moveit.base_frame).
octomap_frame: base_link

# 6 mm voxel. 2mm까지 낮췄다가 D435 자체의 깊이 노이즈 floor(작업 거리
# 30~70cm 에서 수 mm 단위로 흔들림)보다 voxel 이 더 잘게 썰리면서 평면이
# 울퉁불퉁하게 잡히는 문제가 심해져 되돌렸다 — voxel 을 노이즈 floor 보다
# 작게 만들어도 정확도가 좋아지진 않고 흔들림만 그대로 voxel 모양으로
# 드러난다. 노이즈 자체는 motion.py 의 옥토맵 클라우드 디노이즈
# (`_remove_outlier_points`)와 아래 RealSense 필터로 잡는다. voxel 수는
# 세제곱으로 늘어나니(15mm 대비 약 15.6배) CPU/업데이트 지연 체감되면
# 여기부터 올릴 것.
octomap_resolution: 0.006
